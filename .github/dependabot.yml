version: 2
updates:

- package-ecosystem: npm
  directory: "/"
  schedule:
    interval: daily
  labels:
  - dependencies
  commit-message:
    prefix: deps
  allow:
  - dependency-type: "production"
